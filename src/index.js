import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import RandomNumberDisplay from './App';

ReactDOM.render(
  <React.StrictMode>
    <RandomNumberDisplay />
  </React.StrictMode>,
  document.getElementById('root')
);
